apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev
  namespace: librareome
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev
  template:
    metadata:
      labels:
        app: dev
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nautilus.io/vis
                operator: DoesNotExist
              - key: topology.kubernetes.io/region
                operator: In
                values:
                - us-west
      hostname: dev
      containers:
      - name: dev
        image: gitlab-registry.nrp-nautilus.io/ar-noc/arnoc_glx:latest
        env:
        - name: TZ
          value: "UTC"
        - name: SIZEW
          value: "1920"
        - name: SIZEH
          value: "1200"
        - name: CDEPTH
          value: "24"
        - name: SHARED
          value: "TRUE"
        - name: PASSWD
          value: "mypasswd"
#          valueFrom:
#            secretKeyRef:
#              name: gxgl-pass
#              key: gxgl-pass
        - name: VIDEO_PORT
          value: "DFP"
        stdin: true
        tty: true
        ports:
        - containerPort: 8080
          protocol: TCP
        resources:
          limits:
            memory: 64Gi
            cpu: "16"
            nvidia.com/gpu: 1
          requests:
            memory: 100Mi
            cpu: 100m
        volumeMounts:
        - mountPath: /dev/shm
          name: dshm
        - mountPath: /cache
          name: dev-cache-vol
        - mountPath: /home/user
          name: dev-root-vol
        

      volumes:
      - name: dshm
        emptyDir:
          medium: Memory
      - name: dev-cache-vol
        persistentVolumeClaim:
          claimName: dev-cache-vol
      - name: dev-root-vol
        persistentVolumeClaim:
          claimName: dev-root-vol
      - name: dev-sw-vol

     

